# Client-Server-HTTP

## Playbook

* Open VSCode Terminal
*     node server.js
* Go to POSTMAN
*     http://localhost:3000/
* Select the `POST` and `GET` options and send requests
* Observer the Responses
